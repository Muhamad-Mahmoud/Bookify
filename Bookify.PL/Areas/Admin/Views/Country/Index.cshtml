@model List<Country>

<link rel="stylesheet" href="~/css/admin-tables.css" />

<div class="dashboard-page">
    <!-- Page Header -->
    <div class="page-header">
        <h1>
            <i class="fas fa-globe"></i> Countries Management
        </h1>
        <a asp-controller="Country" asp-action="Add" class="add-hotel-btn">
            <i class="fas fa-plus-circle"></i> Add New Country
        </a>
    </div>

    <!-- Countries Table -->
    <div class="hotels-table-container">
        @if (Model.Any())
        {
            <table class="table hotels-table">
                <thead>
                    <tr>
                        <th class="text-center" style="width: 80px;">ID</th>
                        <th>Country Name</th>
                        <th class="text-center" style="width: 200px;">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td class="text-center">
                                <span class="hotel-id-badge">@item.Id</span>
                            </td>
                            <td>
                                <span class="hotel-name">@item.CountryName</span>
                            </td>
                            <td class="text-center">
                                <div class="action-buttons">
                                    <a asp-controller="Country"
                                       asp-action="Edit"
                                       asp-route-id="@item.Id"
                                       class="btn-sm-custom btn-edit"
                                       title="Edit Country">
                                        <i class="fas fa-edit"></i> Edit
                                    </a>

                                    <a asp-controller="Country"
                                       asp-action="Delete"
                                       asp-route-id="@item.Id"
                                       class="btn-sm-custom btn-delete"
                                       title="Delete Country">
                                        <i class="fas fa-trash"></i> Delete
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <div class="no-hotels-message">
                <i class="fas fa-inbox"></i>
                <p>No countries found. <a asp-controller="Country" asp-action="Add">Create your first country</a></p>
            </div>
        }
    </div>
</div>